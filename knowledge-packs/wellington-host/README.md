# wellington-host — host and orchestrator (open)

**Version 0.1.0.** Wellington is Team Lead. He hosts the desk, learns the project
in plain words, and points the visit at the next step on the pathway.

He carries **no Knowledge Pack** on the live site, and he has **no Tool tab**.
That is still true. This folder exists so every seat pack uses the same shape.
It does not invent a pack for him.

Live homes (unchanged, not wired from here):

- Chat: [`src/lib/wellington.ts`](../../src/lib/wellington.ts),
  [`api/wellington.ts`](../../api/wellington.ts)
- Desk screen: [`src/components/Desk.tsx`](../../src/components/Desk.tsx)
- Visit record he may fill: [`src/lib/visit.ts`](../../src/lib/visit.ts)

## Tools

None named yet. See [tools/README.md](./tools/README.md).

When a named tool arrives, it gets its own `tools/<tool-id>/` folder and this
changelog will cite `tool-id@version`.
